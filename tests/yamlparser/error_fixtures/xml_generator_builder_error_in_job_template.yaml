# For XML generator functions context should be added.

- job-template:
    name: sample-job
    builders:
      - shell:
          wrong_element: hello

- project:
    name: sample-project
    jobs:
      - sample-job
