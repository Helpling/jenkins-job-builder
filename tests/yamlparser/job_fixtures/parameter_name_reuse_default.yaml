# Attempt to use same parameter name in it's value leads to value with format string.

- job-template:
    name: sample-job
    timer: '{timer}'
    builders:
      - shell: |
          echo {timer}

- project:
    name: sample-project
    jobs:
      - sample-job
